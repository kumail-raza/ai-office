export default function NotFound() {
  return (
    <main
      style={{
        display: "flex",
        minHeight: "100dvh",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <p>Page not found.</p>
    </main>
  );
}
