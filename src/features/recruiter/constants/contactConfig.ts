/**
 * Central contact configuration. Update these values (not component code) to
 * change where recruiter contact actions point.
 */
export const CONTACT_CONFIG = {
  linkedinUrl: "#",
  githubUrl: "#",
  email: "hello@kumail.dev",
  scheduleUrl: "#",
  resumeUrl: "#",
} as const;
